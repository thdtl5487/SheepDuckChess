export interface SignupDTO {
    loginType: number;
    loginId: string;
    loginPw: string;
    nick: string;
}

export interface LoginDTO{
    loginType: number;
    loginId: string;
    loginPw: string;
}